export interface IAsset {
    assets: [
        {
            name: string;
            description: string;
            brand: string;
            price: string;
            quantity: string;
            category: string;
            id: number;
        }
    ]
}