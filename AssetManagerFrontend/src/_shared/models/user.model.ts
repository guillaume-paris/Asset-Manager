export interface IUser {
    users: [
        {
            firstname: string;
            lastname: string;
            email: string;
            role: string;
            id: number;
        }
    ]
}